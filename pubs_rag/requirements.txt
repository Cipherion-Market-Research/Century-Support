# Publications RAG ingestion + webhook service (WP-4) — deployed
# independently of the bot; own requirements file so this service's
# dependency set doesn't collide with the root bot's or the other WP
# services' (same rationale as kpi_sync/requirements.txt).
aiohttp==3.14.1
asyncpg==0.31.0
pgvector==0.5.0
pypdf==6.14.2
# pypdf's AES decryption path (these PDFs are AES-encrypted with an empty
# user password) needs `cryptography` installed; without it pypdf falls
# back to RC4-only support and decrypt() fails on these files.
cryptography==49.0.0
beautifulsoup4==4.15.0
openai==2.46.0
